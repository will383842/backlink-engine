# ──────────────────────────────────────
# Database (PostgreSQL)
# ──────────────────────────────────────
DATABASE_URL="postgresql://backlink:backlink_secret@localhost:5432/backlink_engine?schema=public"

# ──────────────────────────────────────
# Redis (BullMQ queues + caching)
# ──────────────────────────────────────
REDIS_URL="redis://localhost:6379"
REDIS_PASSWORD="redis_default_password"

# ──────────────────────────────────────
# MailWizz API
# ──────────────────────────────────────
MAILWIZZ_API_URL="https://your-mailwizz-instance.com/api"
MAILWIZZ_API_KEY="your-mailwizz-api-key"

# MailWizz Global Control (overridable via API)
MAILWIZZ_ENABLED=false    # Master switch: enable/disable sending
MAILWIZZ_DRY_RUN=true     # Dry-run mode: log without actually sending

# MailWizz List UIDs (one per language) - REQUIRED
# Create one list per language in MailWizz admin and paste UIDs here
MAILWIZZ_LIST_FR="list_uid_french"
MAILWIZZ_LIST_EN="list_uid_english"
MAILWIZZ_LIST_DE="list_uid_german"
MAILWIZZ_LIST_ES="list_uid_spanish"
MAILWIZZ_LIST_PT="list_uid_portuguese"
MAILWIZZ_LIST_RU="list_uid_russian"
MAILWIZZ_LIST_AR="list_uid_arabic"
MAILWIZZ_LIST_ZH="list_uid_chinese"
MAILWIZZ_LIST_HI="list_uid_hindi"

# ──────────────────────────────────────
# OpenAI API (GPT-4o-mini for LLM features)
# ──────────────────────────────────────
OPENAI_API_KEY="sk-..."

# ──────────────────────────────────────
# Google Safe Browsing API (optional)
# Used for domain safety verification
# ──────────────────────────────────────
GOOGLE_SAFE_BROWSING_API_KEY="your-google-safe-browsing-key"

# ──────────────────────────────────────
# Moz API (optional - for Domain Authority enrichment)
# Leave commented if not using Moz metrics
# ──────────────────────────────────────
# MOZ_ACCESS_ID="your-moz-access-id"
# MOZ_SECRET_KEY="your-moz-secret-key"

# ──────────────────────────────────────
# Open PageRank API (optional - for PageRank enrichment)
# Leave commented if not using PageRank metrics
# ──────────────────────────────────────
# OPEN_PAGERANK_API_KEY="your-openpagerank-key"

# ──────────────────────────────────────
# JWT Authentication
# IMPORTANT: Generate a strong secret (min 32 characters)
# Example: openssl rand -base64 48
# ──────────────────────────────────────
JWT_SECRET="change-me-to-a-long-random-string-min-32-chars"

# ──────────────────────────────────────
# MailWizz Webhook Secret
# Shared secret for webhook signature verification
# ──────────────────────────────────────
MAILWIZZ_WEBHOOK_SECRET="your-mailwizz-webhook-shared-secret"

# ──────────────────────────────────────
# IMAP (reply ingestion)
# Used to monitor inbox for prospect replies
# ──────────────────────────────────────
IMAP_HOST="imap.example.com"
IMAP_PORT=993
IMAP_USER="replies@yourdomain.com"
IMAP_PASSWORD="your-imap-password"

# ──────────────────────────────────────
# Ingest API (webhook security)
# API key for external scraper to submit prospects
# ──────────────────────────────────────
INGEST_API_KEY="your-ingest-api-key"

# ──────────────────────────────────────
# Server Configuration
# ──────────────────────────────────────
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info

# CORS Configuration
# IMPORTANT: In production, set to your frontend domain(s)
# Example: CORS_ORIGIN="https://yourdomain.com,https://admin.yourdomain.com"
CORS_ORIGIN=true

# ──────────────────────────────────────
# Docker Compose (optional overrides)
# These have defaults in docker-compose.yml
# ──────────────────────────────────────
# POSTGRES_USER=backlink
# POSTGRES_PASSWORD=backlink_secret
# POSTGRES_DB=backlink_engine
